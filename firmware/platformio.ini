; Claude Code Meter — ESP32 Firmware
; PlatformIO Project Configuration
;
; Hardware: ESP32-S3 or ESP32-WROOM-32 + MAX7219 4-in-1 Dot Matrix
; Architecture: Headless IoT client polling n8n webhook or direct API

[env]
framework = arduino
monitor_speed = 115200
lib_deps =
    bblanchon/ArduinoJson@^7.0.0
    majicdesigns/MD_Parola@^3.7.0
    majicdesigns/MD_MAX72XX@^3.5.0
    https://github.com/tzapu/WiFiManager.git#v2.0.17
build_flags =
    -DCORE_DEBUG_LEVEL=3
    -DARDUINOJSON_ENABLE_COMMENTS=0
    -DARDUINOJSON_ENABLE_NAN=0

[env:esp32s3]
platform = espressif32
board = esp32-s3-devkitc-1
upload_speed = 921600
build_flags =
    ${env.build_flags}
    -DBOARD_ESP32S3=1

[env:esp32dev]
platform = espressif32
board = esp32dev
upload_speed = 921600
build_flags =
    ${env.build_flags}
    -DBOARD_ESP32DEV=1
